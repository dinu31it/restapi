name: Label PRs

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: Install GitHub CLI
        run: sudo apt-get install gh

      - name: Add current year label
        run: |
          YEAR=$(date +"%Y")
          echo "Adding label: $YEAR"
          gh pr edit "$PR_NUMBER" --add-label "$YEAR"
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run Labeler
        uses: actions/labeler@v4
